# https-to-mq: POST /ingest/hm -> rate-limit (429 over limit) -> produce DEV.HM.SINK -> 202.
name: https-to-mq
routeType: https-mq
source:
  httpPath: /ingest/hm
sink:
  queue: DEV.HM.SINK
rateLimit:
  enabled: true
  key: hm
  rate: 5.0
  burst: 10
